# Skill do hipocampo-toolkit — stub

A arquitetura da(s) skill(s) deste toolkit está **em aberto** — decisão adiada por pedido explícito durante o desenho da metodologia, para ser revisitada depois do Lote 3 (instanciação dos primeiros repositórios de conteúdo reais). Ver `hipocampo` `decisions/` para o histórico de decisões já fechadas.

Este arquivo é um placeholder, não uma implementação. Quando a arquitetura for decidida, ela deve cobrir pelo menos:

- Ritual de consolidação (inbox → documento com frontmatter completo, sempre com revisão humana antes da escrita definitiva — invariante 4 do `CLAUDE.md`).
- Ritual de staleness (checagem de `ttl` vencido, comportamento diferenciado por `temporality` — ver `hipocampo/SPEC.md`, seção 5).
- Resolução de `related` cross-repositório via `registry.md`.

Não usar este stub como base de código — é só registro de escopo pendente.
